const EPSILON = 'ε';
const EPSILON_CLOSURE = `${EPSILON}*`;
const CONCAT: string = '.';
const ETOILE: string = '*';
const ALTERN: string = '|';
const PROTECTION: string = 'PROTECTION';
const PARENTHESEOUVRANT: string = '(';
const PARENTHESEFERMANT: string = ')';
const DOT: string = '.';

export {EPSILON, EPSILON_CLOSURE, CONCAT, ETOILE, ALTERN, PROTECTION, PARENTHESEFERMANT, PARENTHESEOUVRANT, DOT};
